{
  "name": "@skills/sbom-auditor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo component build --release && cp target/wasm32-wasip2/release/sbom_auditor.wasm component.wasm",
    "bundle": "bash ../../scripts/skill-card.sh bundle .",
    "push": "bash ../../scripts/skill-card.sh push .",
    "test": "cargo test",
    "clean": "cargo clean && rm -rf bundle component.wasm .turbo"
  }
}
